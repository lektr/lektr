# =============================================================================
# LEKTR CONFIGURATION
# =============================================================================
# Copy this file to .env and customize as needed.
# Required variables are marked with [REQUIRED], others are optional.

# =============================================================================
# DATABASE [REQUIRED for production]
# =============================================================================
# PostgreSQL connection. These match the docker-compose.yml defaults.
POSTGRES_USER=lektr
POSTGRES_PASSWORD=lektr_dev
POSTGRES_DB=lektr
POSTGRES_HOST=db                 # Use 'db' for Docker, 'localhost' for local dev
POSTGRES_PORT=5432

# Or use a full connection URL (takes priority if set):
# POSTGRES_URL=postgres://lektr:lektr_dev@localhost:5432/lektr

# =============================================================================
# SECURITY [REQUIRED for production]
# =============================================================================
# JWT secret for session tokens. MUST be set in production!
# Generate with: openssl rand -base64 32
# JWT_SECRET=your-very-long-secret-key-at-least-32-chars

# Default admin account created on first run
ADMIN_EMAIL=admin@lektr.local
ADMIN_PASSWORD=admin123

# =============================================================================
# NETWORK
# =============================================================================
# Port for Docker production deployment (Nginx)
LEKTR_PORT=80

# CORS origins (comma-separated). Defaults to localhost variants if not set.
# CORS_ORIGINS=http://localhost,http://192.168.1.100

# =============================================================================
# EXTERNAL SERVICES (Optional)
# =============================================================================
# Hardcover.app API key for high-quality book covers and metadata.
# Get your key at: https://hardcover.app/account/api
# HARDCOVER_API_KEY=your-hardcover-api-key

# =============================================================================
# EMAIL (Optional - can also be configured via Admin Settings UI)
# =============================================================================
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password
# SMTP_SECURE=false              # true for port 465
# MAIL_FROM_NAME=Lektr
# MAIL_FROM_EMAIL=noreply@example.com

# Application URL (used in email links)
# APP_URL=http://localhost

# =============================================================================
# SCHEDULED TASKS
# =============================================================================
# Daily Digest email schedule (cron expression)
# Default: 8 AM daily
# DIGEST_CRON=0 8 * * *

# =============================================================================
# TELEMETRY (Optional)
# =============================================================================
# Lektr collects anonymous usage statistics to improve the product.
# This can be disabled via Admin Settings UI, or by setting:
# POSTHOG_API_KEY=disabled
